/**
 * Icon string to image mapping.
 * Nodes with label matching the map key will load the associated icon.
 */

export const THREAT_ACTOR_ICONS = {
    
    // ** Attacker icons
    threat_actor: require('./threat_actor.png'),
    apt_group: require('./threat_actor.png'),
   
};
